import { createUseStyles } from "react-jss";

export const useLogoStyles = createUseStyles({
  container: {
    fontSize : "48px",
    fontFamily : "Lexend"
  },
  span : {
    color : "#2d3436",
    fontWeight : "bold",
    fontSize : "38px",
    fontFamily : "Lexend"
  }
});
