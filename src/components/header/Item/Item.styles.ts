import { createUseStyles } from "react-jss";

export const useItemStyles = createUseStyles({
  container: {
    display : "inline-block",
    margin : "0px 15px",
    fontFamily : "Lexend"
  },
  a : {
    textDecoration : "none",
    color : "#fff",
    cursor : "pointer",
    fontWeight : "bold",
    letterSpacing : "2px",
    fontFamily : "Lexend"
  }
});
