import { createUseStyles } from "react-jss";

export const useDetailStyles = createUseStyles({
  container: {
    padding : "10px 93px",
    backgroundColor : "#fff",
    lineHeight : "1.5",
    color : "#444",
    fontFamily : "Lexend",
  },
});
